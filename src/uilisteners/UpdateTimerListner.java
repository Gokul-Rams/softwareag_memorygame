package uilisteners;

/**
 * Listenr to update timer
 * @author gokul
 *
 */
public interface UpdateTimerListner {
	/**
	 * listner method to update timmer
	 * currentsec, secound to update on timer 
	 * @param currentsec
	 */
	public void updatetimer(int currentsec);
}
